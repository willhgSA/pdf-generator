<!DOCTYPE html>
<html>
<head>
  <title>{{title}}</title>
  <style>
    body { font-family: Arial, sans-serif; }
    h1 { color: #333; }
    img { max-width: 100%; }
  </style>
</head>
<body>
  <h1>{{title}}</h1>
  <p>{{content}}</p>
  {{#if imageUrl}}
    <img src="file://{{imageUrl}}" alt="Uploaded Image">
  {{/if}}
</body>
</html> 